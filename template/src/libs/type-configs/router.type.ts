import RouterImpl from '../implements/router/router.implement';
import RouterTool from '../implements/router/router.tool';

export default [
    { id: 'routerTool', type: RouterTool },
    { id: 'router', type: RouterImpl }
]
